# gr-utat
Out of tree modules for ground station.

Mandatory dependencies:
* swig

To install, use the following commands:
* cd gr-utat
* mkdir build
* cd build
* cmake ..
* make
* sudo make install
* sudo ldconfig